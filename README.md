Using Github:
BEFORE DOING ANYTHING:
in terminal:
cd GoodParty
git status

output of git status should say origin/yourname
if not run:
git switch yourname

if that does not work:
git checkout yourname

Then to push your changes run:
git add .
git commit -m "Name of your Commit"
git push
